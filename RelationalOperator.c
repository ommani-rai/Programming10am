#include <stdio.h>
#include <conio.h>

int main()
{
    /*
    multiline comment
    */
    // ctrl + /
    //  <, >, <=, >=, ==, !=
    int a = 73;
    int b = 53;
    // less than
    // if (a < b)
    // {
    //     printf("%d is less than %d\n", a, b);
    // }

    // greater than
    // if (a > b)
    // {
    //     printf("%d is less than %d\n", a, b);
    // }

    // less than or equalls to
    // if(a<=b){
    //     printf("%d is less than or equall to %d\n", a , b);
    // }

    // greater than or equall to
    // if(a>=b){
    //     printf("%d is less than or equall to %d\n", a , b);
    // }

    // equals to equals to
    // if(a==b){
    //     printf("%d is less than or equall to %d\n", a , b);
    // }


    // not equalls to
    if(a!=b){
        printf("%d is  equal to %d\n", a , b);
    }


    printf("Hello world");
    return 0;
}