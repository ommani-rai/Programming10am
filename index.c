#include<stdio.h>

int main(){
    printf("Hello world!\n");
    // single line comment
    /*
    multi line comment

    types of programming languages:
    1. low level
        a. machine language(1, 0 (binary digits))
        b. assembly language (add(1110), sub, div )
    2. high level
        c, python, c++, java, javascript


    variable:
    float firstname = 232.2323;
    variable is the container which stores the data
    variable is process of memory allocation

    rules for declaring a variable:
    1. always start with alphabet or underscore(_)
    2. does not start with number or special character
    3. does not include white space
    4. does not use keyword                          

    naming convention:
   firstName: camel case
   Firstname: pascal case
   first_name: snake case
   first-name: kebab case
    */

   int a = 10;
   int b = 12;
   printf("the value of a is: %d\n ", a);
   printf("the value of b is: %d\n", b);



    return 0;
}
